{
  "name": "max-line-length",
  "version": "1.0.0",
  "description": "Get maximum line length of a string",
  "main": "max-line-length-cjs.js",
  "files": [
    "max-line-length-cjs.js"
  ],
  "scripts": {
    "test": "node test.js",
    "coverage": "istanbul cover test.js",
    "pretest": "eslint --config node_modules/@shinnn/eslintrc/rc.json --ignore-path .gitignore . && bower install",
    "coveralls": "${npm_package_scripts_coverage} && istanbul-coveralls"
  },
  "author": "Shinnosuke Watanabe && https://github.com/shinnn",
  "repository": "shinnn/max-line-length.js",
  "license": "Unlicense",
  "keywords": [
    "string",
    "line",
    "length",
    "number",
    "max",
    "maximum",
    "browser",
    "client-side"
  ],
  "dependencies": {
    "line-length": "^1.0.1"
  },
  "devDependencies": {
    "@shinnn/eslintrc": "^1.0.0",
    "bower": "^1.4.1",
    "eslint": "^0.22.1",
    "istanbul": "^0.3.15",
    "istanbul-coveralls": "^1.0.3",
    "require-bower-files": "^2.0.0",
    "tape": "^4.0.0"
  }
}
