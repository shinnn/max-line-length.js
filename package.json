{
  "name": "max-line-length",
  "version": "1.0.0",
  "description": "Get maximum line length of a string",
  "main": "./max-line-length-cjs.js",
  "files": [
    "max-line-length-cjs.js",
    "LICENSE"
  ],
  "scripts": {
    "pretest": "eslint *.js & jscs *.js && bower install",
    "test": "node test.js",
    "coverage": "istanbul cover test.js",
    "coveralls": "${npm_package_scripts_coverage} && istanbul-coveralls"
  },
  "author": {
    "name": "Shinnosuke Watanabe",
    "url": "https://github.com/shinnn"
  },
  "repository": "shinnn/max-line-length.js",
  "license": "Unlicense",
  "keywords": [
    "string",
    "line",
    "length",
    "number",
    "max",
    "maximum",
    "browser",
    "client-side"
  ],
  "dependencies": {
    "line-length": "^1.0.0"
  },
  "devDependencies": {
    "bower": "^1.3.12",
    "eslint": "^0.9.2",
    "istanbul": "^0.3.2",
    "istanbul-coveralls": "^1.0.1",
    "jscs": "^1.7.3",
    "require-bower-files": "^1.0.0",
    "tape": "^3.0.3"
  },
  "jscsConfig": {
    "preset": "google"
  }
}
